<!DOCTYPE html>
<html>
  <head>
    <title>JebzelTest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="header">
      <h1>My Blog</h1>
    </div>
    <div class="row">
      <%= yield %>
    </div>

    <div class="footer">
      <h2>Jebzel Castillo | <%= DateTime.now().strftime('%d %b %Y') %></h2>
    </div>
  </body>
</html>
